export interface LoginForm {
    email: string;
    password: string;
}

export interface RegisterForm {
    firstName: string;
    lastName: string;
    phoneNumber: string;
    userType: string;
    email: string;
    password: string;
    confirm_password: string;
}